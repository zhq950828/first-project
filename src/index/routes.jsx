import React from 'react';
import { Route } from 'react-router';
import Index from './index';

const routes = (
    <Route
        key="Index"
        path="index"
        component={Index}
    >
    </Route>
);

export default routes;